@model IEnumerable<WebApplicationTest.Entities.Empleado>

@{
    ViewData["Title"] = "TodoEnUno";
}

<h1>TodoEnUno</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Nombre)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Apellido)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Salario)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Nacimiento)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Nombre)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Apellido)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Salario)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Nacimiento)
                </td>
                <td>
                    <a asp-action="Edit" asp-route-id="@item.EmpleadoId">Edit</a> |
                    <a asp-action="Details" asp-route-id="@item.EmpleadoId">Details</a> |
                    <a asp-action="Delete" asp-route-id="@item.EmpleadoId">Delete</a>
                </td>
            </tr>
        }
    </tbody>
</table>

<!-- Button trigger modal -->
<button type="button" class="btn btn-primary" data-bs-toggle="modal" data-bs-target="#exampleModal">
    Launch demo modal
</button>

<!-- Modal -->
<div class="modal fade" id="exampleModal" tabindex="-1" aria-labelledby="exampleModalLabel" aria-hidden="true">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="exampleModalLabel">Modal title</h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>
            <div class="modal-body">
                ...
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Close</button>
                <button type="button" class="btn btn-primary" onclick="Grabar();">Grabar</button>
            </div>
        </div>
    </div>
</div>

<div id="target"></div>

<script>
    function grabar() {
        var nombre = $('#Nombre')[0].value;
        var apellido = $('#Apellido')[0].value;
        var salario = $('#Salario')[0].value;
        var nacimiento = $('#Nacimiento')[0].value;
        var data = {
            Nombre: nombre,
            Apellido: apellido,
            Salario: salario,
            Nacimiento: nacimiento
        };
        $.post('/ejemplo/crear', data)
            .done(function (rseultado) {
           // $('#target')[0].html(resultado);
            if (resultados) {                
                alert('Se grabao');
            }
        })
        .fail(function () {
            //TODD
        });
    };
</script>